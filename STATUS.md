# Lumiere — Статус разработки

> Последнее обновление: 2026-07-25
> Ветка: master (commit dd2a08e)

---

## Что сделано

### Фаза 3: Онлайн-источники ✅

**Провайдеры:**
- **Collaps** — полная интеграция через API (`api.bhcesh.me`), поиск, получение HLS/DASH потоков
- **HDVB** — интеграция через API (`apivb.com`), поиск по названию
- **Phantom** — отключён (домен не резолвится)

**API эндпоинты:**
- `GET /api/online/search?q=...&type=movie|series` — поиск по всем провайдерам
- `GET /api/online/stream/:provider/:id` — получение URL потоков
- `GET /api/online/proxy?provider=...&id=...` — HLS прокси с перезаписью URL

### Фаза 4: Торренты + Плеер ✅

**Торрент-интеграция:**
- **JacRed** — удалённый API (`ns3bg91xvuqfvq9h.cfhttp.top`) для поиска торрентов
- **TorrServer** — стриминг торрентов (контейнер на порту 8090)
- **qBittorrent** — менеджер загрузок (контейнер на порту 6003)

**API эндпоинты:**
- `GET /api/torrents/search?q=...` — поиск через JacRed
- `POST /api/torrents/stream` — добавление торрента в TorrServer + получение файлов
- `GET /api/torrents/proxy?link=...&index=...` — прокси для стриминга
- `GET /api/torrents/hls?link=...&index=...` — HLS транскодирование через FFmpeg
- `GET /api/torrents/hls-seek?link=...&index=...&time=...` — перемотка (перезапуск FFmpeg)
- `GET /api/torrents/duration?link=...&index=...` — длительность видео через FFprobe
- `GET /api/torrents/jacred/status` — статус JacRed
- `GET /api/torrents/torrserver/status` — статус TorrServer

**Видеоплеер (Dune-стиль):**
- HLS.js с поддержкой адаптивного битрейта
- Перемотка (клик по таймлайну, перетаскивание, стрелки ±10с)
- Выбор качества (авто/720p/1080p)
- Выбор аудиодорожки
- Субтитры (WebVTT из FFmpeg)
- Громкость, mute, полноэкранный режим
- Сохранение позиции воспроизведения
- Отображение имени серии

**HLS транскодирование:**
- FFmpeg ремуксит MKV/AVI → HLS (сегменты .ts)
- Видео: copy (без перекодирования)
- Аудио: AAC 192k стерео
- Субтитры: WebVTT (если есть в оригинале)
- Сегменты: 6 секунд, VOD режим (все сегменты)

---

## Архитектура

```
Клиент (React + HLS.js)
    ↓
Backend (Fastify)
    ├── /api/online/* → Collaps, HDVB провайдеры
    ├── /api/torrents/* → JacRed + TorrServer + FFmpeg
    └── /api/movies/*, /api/tv/* → TMDB метаданные
    ↓
Сервисы (Docker)
    ├── PostgreSQL (порт 5433)
    ├── TorrServer (порт 8090)
    ├── qBittorrent (порт 6003)
    └── Jackett (порт 6004, не используется)
```

---

## Что работает

1. ✅ Поиск фильмов/сериалов через TMDB
2. ✅ Онлайн-источники (Collaps, HDVB)
3. ✅ Поиск торрентов через JacRed
4. ✅ Стриминг торрентов через TorrServer + FFmpeg
5. ✅ Видеоплеер с перемоткой, качеством, субтитрами
6. ✅ Сохранение позиции просмотра
7. ✅ Отметка просмотренных серий
8. ✅ Настройки JacRed, TorrServer, qBittorrent

---

## Известные проблемы

1. **Collaps CDN** — токены истекают быстро, HLS прокси решает эту проблему
2. **HDVB** — иногда не возвращает результаты (зависит от API)
3. **JacRed индексы** — некоторые трекеры могут быть недоступны (сетевые проблемы)
4. **FFmpeg транскодирование** — занимает несколько секунд при первом запуске

---

## Следующие шаги

1. **Доработать онлайн-источники** — добавить больше провайдеров (Kinobase, FanCDN, Mirage и др.)
2. **Улучшить перемотку** — кэширование сегментов, предзагрузка
3. **Добавить выбор перевода** — для торрентов с несколькими озвучками
4. **Синхронизация** — сохранение позиции на сервере (не только localStorage)
5. **Android/TV** — Capacitor приложение
6. **Субтитры** — внешние субтитры (OpenSubtitles)
7. **Транскодирование** — поддержка разных качеств (720p, 1080p)

---

## Docker-сервисы

| Сервис | Порт | Статус |
|--------|------|--------|
| PostgreSQL | 5433 | ✅ Работает |
| TorrServer | 8090 | ✅ Работает |
| qBittorrent | 6003 | ✅ Работает |
| Jackett | 6004 | ⚠️ Не используется |

---

## Конфигурация

**JacRed:**
- URL: `http://ns3bg91xvuqfvq9h.cfhttp.top`
- Настройки: Settings → Плагины → JacRed

**TorrServer:**
- URL: `http://localhost:8090`
- Настройки: Settings → Плагины → TorrServer

**FFmpeg:**
- Установлен в системе
- Используется для HLS транскодирования
- Параметры: `-c:v copy -c:a aac -b:a 192k -ac 2`

---

*Создано: 2026-07-25*
*Проект: Lumiere*
